<resources>
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@color/white</item>
    </style>

    <style name="ToolbarPrimary" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="colorAccent">@color/white</item>
        <item name="searchViewStyle">@style/ProductSearchView</item>
    </style>

    <style name="RobotoItalicFont">
        <item name="android:fontFamily">@font/roboto_italic</item>
    </style>

    <style name="RobotoBoldFont">
        <item name="android:fontFamily">@font/roboto_bold</item>
    </style>

    <style name="BottomSheetDialog" parent="Theme.MaterialComponents.Light.BottomSheetDialog">
        <item name="android:colorBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.5</item>
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <style name="Spinner">
        <item name="android:colorBackground">@color/white</item>
    </style>

    <style name="ProductSearchView" parent="Widget.AppCompat.SearchView">
        <item name="android:colorAccent">@color/white</item>
        <item name="searchHintIcon">@null</item>
        <item name="closeIcon">@null</item>
        <item name="queryBackground">@android:color/transparent</item>
        <item name="submitBackground">@android:color/transparent</item>
    </style>
</resources>
